<?xml version="1.0" encoding="utf-8"?>
<mx:Canvas xmlns:mx="http://www.adobe.com/2006/mxml" width="540" height="90" styleName="InnerBorder">
	<mx:Script>
		<![CDATA[
			import com.hifong.war.util.tooltip.ArmyTooltip;
			import com.hifong.war.util.ArmyUtil;
			import com.hifong.war.util.WindowUtil;
			import mx.events.ToolTipEvent;
			import com.hifong.war.util.tooltip.BuildArmyTooltip;
			import com.hifong.war.util.ImagePathUtil;
			import com.hifong.war.constant.GlobalConstant;
			import com.hifong.war.model.ModelLocator;
			import com.hifong.war.vo.CityArmyVO;
			import com.hifong.war.vo.ArmyVO;
			import mx.managers.PopUpManager;
			
			private var model:ModelLocator = ModelLocator.getInstance();
			
			private var _army:ArmyVO;
			
			[Bindable]
			public var cityArmy:CityArmyVO;
			
			//是否满足创建条件
			[Bindable]
			private var canCreate:Boolean;
			
			[Bindable]
			public function set army(a:ArmyVO):void{
				this._army = a;
				canCreate = ArmyUtil.meetArmyDepend(a);
			}
			
			public function get army():ArmyVO{
				return this._army;
			}
			
			private function doAssemble():void{
				var win:AssemblePlaneWindow = new AssemblePlaneWindow();
				win.army = army;
				win.cityArmy = cityArmy;
				
				WindowUtil.showModelWindow(win);
			}
			
			private function doDisassemble():void{
				var win:DisassemblePlaneWindow = new DisassemblePlaneWindow();
				win.army = army;
				win.cityArmy = cityArmy;
				
				WindowUtil.showModelWindow(win);
			}
			
			private function showTooltip(event:ToolTipEvent):void{
				canCreate = ArmyUtil.meetArmyDepend(army);
				event.toolTip = new BuildArmyTooltip("组装",army);
			}
			
			private function showArmyTooltip(event:ToolTipEvent):void{
				event.toolTip = new ArmyTooltip(army);
			}
			
		]]>
	</mx:Script>
	<mx:Button x="82" y="28" label="组装" id="btnArm" enabled="{canCreate}" click="doAssemble()" width="60"  toolTip=" " toolTipCreate="showTooltip(event)" height="26"/>
	<mx:Button x="82" y="56" label="拆卸" id="btnDisarm" click="doDisassemble()" enabled="{cityArmy.num>0}" width="60" height="26"/>
	<mx:TextArea x="150" y="30" width="380" height="50" text="{army.description}" editable="false">
	</mx:TextArea>
	<mx:Label x="299.5" y="10" text="数量：" fontWeight="bold"/>
	<mx:Text x="339.5" y="10" text="{cityArmy.num}" width="160" id="txtSoldierNum" fontWeight="bold"/>
	<mx:Image x="10" y="20" width="60" height="60" source="{ImagePathUtil.getBuildArmyPath(army.image)}" toolTip=" " toolTipCreate="showArmyTooltip(event)"/>
	<mx:Label x="150" y="7" text="{army.name}" width="141.5" textAlign="left" fontWeight="bold"/>
</mx:Canvas>
